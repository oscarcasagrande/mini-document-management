Artefatos de contrato exportados da API.

O OpenAPI é gerado pelo código e servido em `/swagger/v1/swagger.json`; exporte com:

    curl -s http://localhost:8080/swagger/v1/swagger.json -o docs/api/openapi-v1.json
